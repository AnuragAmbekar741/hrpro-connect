export interface MenuItem {
  id: string;
  label: string;
}

export interface PronounOption {
  id: string;
  label: string;
}

export interface CountryCodeOption {
  name: string;
  dial_code: string;
  emoji: string;
  code: string;
}